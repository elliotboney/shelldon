---
topic: Expand + pressure-test OpenClawGotchi v2 architecture (E-Ink AI pet, Pi Zero 2W ~512MB, remote LLMs)
goal: Stretch the design wider and stress-test specific ideas/assumptions
mode: autonomous
status: complete
updated: 2026-06-15T20:47
---

- (technique by coach) started How Might We (reframe)
- (question by coach) HMW make the pet feel ALIVE between turns when the brain is ephemeral and nothing cognitive is resident?
- (question by coach) HMW keep peak RAM bounded WITHOUT paying Python cold-start every turn?
- (question by coach) HMW make the broker's safety policy auditable by non-programmer future-Elliot?
- (question by coach) HMW survive a remote-LLM outage gracefully on the E-Ink face?
- (question by coach) HMW make adding a new channel a 30-minute job?
- (question by coach) HMW test the whole turn loop with no real hardware and no real LLM?
- (question by coach) HMW bound token cost per day with remote LLMs?
- (idea by coach) Resident 'reflexes' in core: rule-based micro-behaviors (blink, idle, time-of-day mood) needing no LLM, driven by display service on a timer
- (idea by coach) Mood cache: worker emits next-likely faces/quips at end of turn; core plays them on triggers without spawning
- (idea by coach) Personality-state struct kept in core (mood/energy/last-interaction); reflexes read it so the SOUL persists though the brain is ephemeral
- (idea by coach) Fork-server: parent pre-imports LLM libs once, os.fork() per turn; COW shares imported pages, child death frees them — warm-start speed + bounded RAM
- (idea by coach) Lazy tool imports in worker: only import modules named in Job.allowed_tools
- (idea by coach) Strip worker to raw provider httpx client instead of LiteLLM to cut import weight
- (idea by coach) Pin worker to a memory cgroup; OOM-kill the worker, never the core
- (insight by coach) Fork-server may beat the doc's recommended 'recycle' worker — resolves the central cold-start-vs-RAM contradiction more cleanly
- (technique by coach) started Brain Writing Round Robin (build on broker / bus / display seeds)
- (idea by coach) Broker is the ONLY holder of credentials; workers never see API keys — broker even proxies the LLM call so the model call itself is brokered (cost+audit)
- (idea by coach) Broker emits a structured audit stream the web UI tails live: 'watch your pet think'
- (idea by coach) Broker policy is DATA (declarative ruleset file), not code — policy changes need no worker/core rebuild
- (idea by coach) Broker dry-run mode: replay a Job, show what side effects WOULD fire — policy testing
- (idea by coach) Broker enforces per-conversation rate limits + token budgets (backpressure lives here too)
- (idea by coach) Bus persists envelopes to sqlite (WAL) so a crash mid-turn can replay the trigger
- (idea by coach) Bus 'coalesce': rapid messages from one conversation merge into one Job (answers the backpressure open-question)
- (idea by coach) Bus is the integration-test seam: inject synthetic envelopes, assert Results
- (idea by coach) Outbound bus too: directives flow back through the same bus to adapters/display (symmetry)
- (idea by coach) Envelope/Job/Result carry schema_version from day 1 for the future Go swap
- (idea by coach) Display owns a framebuffer model + diffs; push only changed regions (partial E-Ink refresh) to cut refresh time
- (idea by coach) Display directive queue with priority: urgent faces preempt ambient animations
- (idea by coach) Display snapshots PNG to a ring buffer -> web mirror + 'what did the pet show at 3am' debugging
- (idea by coach) Display has a hardware-mock backend (renders PNG only) so dev happens on a laptop with no GPIO
- (idea by coach) Faces as declarative layered sprites (eyes/mouth/accessory) composited at render -> fewer assets, more expressions
- (technique by coach) started Worst Possible Idea (generate terrible, flip to lesson)
- (idea by coach) WORST: load LLM into the always-on core for speed -> FLIP: enforce LLM-free core with a CI import-linter that FAILS the build if core/ imports forbidden modules
- (idea by coach) WORST: give worker direct shell+fs 'power' -> FLIP: worker runs as unprivileged user, no network except broker socket + LLM endpoint
- (idea by coach) WORST: one giant process -> FLIP: crash-isolation confirmed, but keep IPC cheap so the split doesn't cost too much (batch broker calls)
- (idea by coach) WORST: all memory in RAM for speed -> FLIP: disk-backed sqlite is the bound; memory queried lazily, never fully resident
- (idea by coach) WORST: block core while worker runs -> FLIP: core stays async/responsive; only agent EXECUTION serializes, receiving never does
- (idea by coach) WORST: full E-Ink refresh every face change -> FLIP: partial refresh + reflex animations; full refresh only on big mood shifts
- (idea by coach) WORST: no tests, ship it -> FLIP: pure-worker design exists to be testable; write the test harness in M0, not M5
- (idea by coach) WORST: let pet edit core/ live and self-restart unsupervised -> FLIP: core self-mod needs write->validate->supervised-restart with ROLLBACK; worker self-mod is safe (next spawn)
- (idea by coach) WORST: spawn unlimited workers under load -> FLIP: 1-slot turn queue + coalesce/drop backpressure
- (technique by coach) started Constraint Mapping (real vs imagined, then attack each)
- (idea by coach) REAL: 512MB RAM -> attack: peak = core + 1 worker, cgroup cap, fork-server shared pages
- (idea by coach) REAL: E-Ink refresh is seconds -> attack: partial refresh, reflexes, show a 'thinking' face instantly while the worker runs
- (idea by coach) REAL: Python cold-start 0.3-1s -> ROUTE AROUND not accept: fork-server / recycled / prefork
- (idea by coach) REAL: remote-LLM network dependency -> attack: offline reflex mode, cached responses, 'sleeping/no-signal' face
- (idea by coach) REAL (often missed): SD-card write wear from sqlite -> attack: WAL, batched writes, tmpfs for ephemera, periodic flush
- (idea by coach) IMAGINED: 'must use LiteLLM' -> dissolve, raw provider HTTP is lighter
- (idea by coach) IMAGINED: 'need Redis/message broker' -> dissolve, in-process asyncio bus + sqlite is enough at this scale
- (idea by coach) IMAGINED: 'self-mod needs resident agent' -> dissolve, apply-on-next-spawn works
- (insight by coach) SD-card wear is a real constraint the doc misses — belongs in M2 memory design
- (technique by coach) started TRIZ Contradiction (win both sides, refuse the trade-off)
- (idea by coach) Cold-start vs RAM -> fork-server: warm shared imports (COW) + child frees on exit = fast AND bounded. Dissolves the doc's central tradeoff
- (idea by coach) Long-lived display (fast) vs crash-isolation -> keep service dead-simple (no LLM, tiny surface) + aggressive watchdog + checkpoint last-good face to replay on respawn
- (idea by coach) Broker chattiness vs broker-everything safety -> batch a turn's side effects into one transactional broker request where ordering allows; keep socket local
- (idea by coach) Powerful agent vs locked-down agent -> broad read-only tools in-process + privileged tools brokered (TRIZ confirms the split is right)
- (idea by coach) Rich memory vs low RAM -> sqlite FTS5 lazy retrieval loads only the relevant slice into the Job; crystallization runs in a worker not resident
- (idea by coach) Multi-channel concurrency vs single-slot serialization -> adapters receive async, only turns serialize, queue+coalesce keeps it responsive
- (technique by coach) started Time Horizon Ladder (1yr / 10yr / 100yr — what survives, breaks, emerges)
- (idea by coach) 1yr SURVIVES: the contracts (envelope/job/result), broker, ephemeral workers — the durable bets
- (idea by coach) 1yr NEEDS WORK: cold-start policy settled empirically; a 2nd adapter proves the gateway claim
- (idea by coach) 10yr: remote-LLM landscape totally different but the worker's LLM client is the only volatile part; JSON seam swaps the worker, core untouched
- (idea by coach) 10yr: on-device LLM may become viable (non-goal today) — worker just calls localhost instead of remote; the split doesn't care where the model lives
- (idea by coach) 10yr: color E-Ink / OLED -> only the display service changes; directives stay device-agnostic
- (idea by coach) 100yr: pet OUTLIVES the hardware — soul/persona/memory are portable data, so the being moves to any future body. Architecture accidentally makes the pet hardware-immortal
- (idea by coach) 100yr: many pets share one memory substrate — conversation partition key generalizes to a 'family' of pets on one vault
- (idea by coach) 100yr: the contracts become a small open protocol others implement ('OpenClawGotchi protocol') — the language-neutral seam IS the product
- (idea by coach) Extra: golden-transcript tests — record real Job+Result, replay vs worker, assert directives (prompt/tool-loop regression safety)
- (idea by coach) Extra: broker simulator for CI — approves/denies per policy with no real side effects
- (idea by coach) Extra: unknown directive types are logged+ignored by core (forward-compat for new face/say/deliver kinds)
- (idea by coach) Extra: surface cost/budget on the pet itself — 'tokens today' on web UI, a tired face when budget runs low
- (insight by coach) Identity-as-portable-data: keep persona/memory strictly data, never entangled with runtime — future-proofs across bodies AND on-device LLMs
- (insight by coach) Brokering the LLM call itself (not just tools) extends the single-security-boundary principle to credentials + cost + audit
- (insight by coach) A CI import-linter forbidding LLM imports in core/ turns design-principle #1 from aspiration into a mechanically-enforced invariant
- (insight by coach) Resident reflexes + a personality-state struct in core is the missing piece for 'feels alive' under an ephemeral-brain design — should be a first-class core component
- (insight by coach) SYNTHESIS: fork-server + personality-state struct + brokered LLM call = one upgraded core (soul + warm-spawn engine + sole keyholder), not the doc's dumb router
- (insight by coach) SYNTHESIS: bus-persists-envelopes + 1-slot-queue/coalesce + crash-isolation = a single durability story ('nothing lost on crash, nothing stampedes under load') worth stating as an explicit goal
- (insight by coach) SYNTHESIS: import-linter + brokered-LLM-call + unprivileged-worker-user are one principle wearing 3 hats — 'the boundary is mechanically enforced, never trusted' (stronger principle #2)
- (insight by coach) SYNTHESIS: identity-as-portable-data is the real breakthrough — it neutralizes the on-device-LLM non-goal and makes the JSON contracts the durable product
- (decision by coach) Next step: distill these into bmad-spec / bmad-prd input; fork-server + reflexes + brokered-LLM are the top design changes to fold into ARCHITECTURE_V2
- (direction by coach) SCOPE CORRECTION: not reinventing openclaw; goal is to rebuild openclawgotchi ground-up with new ideas. Drop 100-yr speculation. Focus: (a) does the v2 tech make sense, (b) what features a Pi Zero could handle that v1 lacks
- (insight by coach) GROUNDED: v1 openclawgotchi is feature-RICH (11KLOC, 40+ tools, dual-brain, vault, voice-in, vision, discord, self-mod, XP, heartbeat). 'Too many features stripped' isn't the real problem
- (insight by coach) TECH VERDICT: every v2 move maps to a real v1 pain — ephemeral workers vs documented OOM crashes; one broker vs safety scattered in 1513-line litellm_connector; long-lived display vs per-face-change subprocess spawn; bus vs bolted-on discord thread; msgspec+tests vs zero tests
- (insight by coach) KEY: only the ephemeral-worker process model is NOT retrofittable into v1; broker/display/contracts could be incremental refactors. So a full rewrite is justified ONLY IF the RAM/ephemeral model is the load-bearing goal
- (insight by coach) REFRAME: v1 stripped OpenClaw into a chatbot-with-a-screen. The Pi Zero's unused headroom is GPIO/embodiment, not compute. Missing features = physical-pet features OpenClaw never had (ran on a Mac): sensors, buttons, sound-out, presence/BLE, proactive reflexes. Embodiment is THE fundamental change available
- (direction by coach) REAL DRIVER surfaced: Elliot wants something that is HIS, not beholden to an upstream maintainer (issues turned off). Motivation is autonomy, not just tech
- (insight by coach) OWNERSHIP unpacked into 3: (1) legal ownership=already yours if MIT/Apache, (2) upstream-independence=a decision not a rewrite, (3) architectural ownership=the ONLY one a rebuild buys. Recommendation: own the spine (contracts/core/broker/bus/display/worker), lift the guts from v1 (faces, tool impls, memory/vault, safety lists)
- (question by coach) OPEN: what is OpenClaw's license, and did Elliot write openclawgotchi or inherit it? Determines whether 'make it mine' needs any action at all
- (insight by coach) RESOLVED: openclawgotchi IS MIT-licensed (Copyright 2024-2026 OpenClawGotchi Contributors, created by Dmitry Turmyshev). Elliot may lawfully lift, modify, or rebuild freely; only must keep the notice in verbatim copies
- (decision by coach) FINAL DECISION: build sheldon ground-up. Reason is ENJOYMENT of building from scratch, not legal/architectural necessity. v1 = reference not source. Attribute Dmitry (README/NOTICE + MIT notice)
- (decision by coach) BUILD SUBSTANCE: spine = ephemeral workers + one broker + envelope bus + long-lived display + msgspec/tests. Fold in fork-server, resident reflexes + personality-state struct, broker-the-LLM-call, CI import-linter on core/. Fundamental new direction = EMBODIMENT (sensors/buttons/sound-out/BLE presence/proactive reflexes)
